{
    "configurations": [
        {
            "name": "React & Vue 源码调试模式",
            "port": 9222,
            "request": "attach",
            "type": "chrome",
            "webRoot": "${workspaceFolder}"
        }
    ]
}